export default class AudioWorkletProcessor {
  // This is a base class for users to extend.
  // The actual implementation is in the worklet's scope.
  // The `process` method will be defined by the user.
}
